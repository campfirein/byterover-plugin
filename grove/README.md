# Grove plugin (v0.4.0)

Generated by `@byterover/grove-plugin`. Do not edit these files by hand: edit the source in
`apps/plugin` and rebuild.

## Install

Claude Code:

```bash
claude plugin marketplace add campfirein/grove-plugin
```

Other hosts read the same directory through the Agent Plugins format. See each host's own
plugin setup instructions.

## Contents

- `skills/grove-company-brain/SKILL.md`: how an agent should use the Company Brain.
- `mcp.json` and `.mcp.json`: the MCP servers this plugin installs. Currently a sample server.
